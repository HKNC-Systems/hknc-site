version: '3.3'

services:
  nginx:
    image: nginx:latest
    container_name: hknc-nginx
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./hknc_site:/usr/share/nginx/html/hknc
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
    restart: always
